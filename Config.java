package lesson;

public class Config {
    public static int windowWidth = 1080;           // Width of window
    public static int windowHeight = 800;           // Height of window
    public static int popupWidth = 480;             // Width of popup
    public static int popupHeight = 320;            // Height of popup
    public static int smallButtonWidth = 60;        // Width of small button
    public static int smallButtonHeight = 35;       // Height of small button
    public static int bigButtonWidth = 120;         // Width of big button
    public static int bigButtonHeight = 75;         // Height of big button
    public static int cmdWidth = 860;               // Width of pseudo cmd
    public static int cmdHeight = 640;              // Height of pseudo cmd
    public static int layoutSpacing1 = 10;          // Spacing configuration 1 for layout
    public static int layoutSpacing2 = 50;          // Spacing configuration 1 for layout
    public static double animationTime = 0.05;      // Time for each animation in seconds

    // Quiz questions and answers for DoS
    public static String[] dosQuiz = {"DoS is abbreviated as Denial of Server / False",
                                     "DDoS is abbreviated as Distributed Denial of Service/ True",
                                     "DoS attacks by flooding the target with traffic / True",
                                     "Buffer overflow attack is NOT a type of DoS / False",
                                     "DDoS is more dangerous than DoS / True"};
}
